[package]
name = "gamesynth-wasm"
description = "WebAssembly build of the gamesynth jet engine with a plain C ABI (no bindgen)"
version.workspace = true
edition.workspace = true
license.workspace = true
publish = false

[lib]
crate-type = ["cdylib"]

[dependencies]
gamesynth-core = { path = "../gamesynth-core" }
serde_json = "1"
